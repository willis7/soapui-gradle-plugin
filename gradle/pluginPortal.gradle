group = 'io.byteshifter'
version = "${soapUIVersion}.RELEASE"

pluginBundle {
    website = projectUrl
    vcsUrl = "${projectUrl}.git"
    description = info
    tags = [
        'soapui',
        'testing',
        'soap',
        'rest',
        'testrunner',
    ]

    plugins {
        soapuiPlugin {
            id = "${group}.soapui"
            displayName = info
        }
    }
}
